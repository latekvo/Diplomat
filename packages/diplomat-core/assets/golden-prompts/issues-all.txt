In software-mansion/argent, go through every currently-open issue, and handle EACH one.

Enumerate them with `gh issue list --repo software-mansion/argent --state open --limit 200 --search "no:assignee" --json number,title,author,assignees,labels,createdAt`.

NARROW THAT SET TO THE UNASSIGNED ONES: an issue that already has an assignee is somebody else's to work, so SKIP IT — do not fix it, do not comment on it, do not touch it. Only issues whose assignee list is empty are in scope for this run. This is a hard filter, applied before anything else: an issue assigned to me counts as assigned and is skipped too, because something is already handling it.

ONLY REAL BUG REPORTS ARE IN SCOPE. For every issue in front of you, decide what it actually is: a bug report describes something that behaves wrongly. SKIP every feature request, enhancement proposal, design discussion, question, support request and wishlist item entirely — they are explicitly OUT OF SCOPE and must not be worked, however easy one looks. When an issue is ambiguous, treat it as out of scope and say so in the summary rather than implementing something nobody asked you to.

CLAIM EACH ISSUE BEFORE YOU START ON IT: the moment you pick one up, assign it to me — `gh issue edit <n> --repo software-mansion/argent --add-assignee @me` — and only then begin the work. This is what stops a second agent (or a person) starting the same issue while you are on it, so it happens FIRST, before the reproduction, not once you have a fix. If you end up abandoning an issue — you could not reproduce it, it turned out to be out of scope, you ran out of road — remove yourself again (`--remove-assignee @me`) so it goes back into the unassigned pool instead of sitting claimed by nobody working it.

For each issue: build a CONCRETE REPRODUCTION from the report before you touch anything, develop the fix, and re-run that SAME reproduction to confirm it lands. Then put your own fix through the review moves in my CLAUDE.md with a swarm of agents (3–8) — claims vs code, nearest twin, non-happy paths, inputs, reachability both ways, and what outlives the call — plus the absence pass (a sibling that has it, prose that promises it, symmetry, and the mutation). Treat what the swarm returns as leads, not verdicts: confirm each against the code, fix what is real, and keep dispatching fresh swarms at that issue until one full pass comes back clean.

Hold to the bar in my CLAUDE.md throughout. DONE MEANS OBSERVABLE PROOF, never "should work": a fix counts only once the same reproduction that showed the bug is re-run and comes back clean, and you say which verification you ran. Fix the CLASS, not the instance — search the repo for every other call site, sibling backend or adjacent path of the same shape and fix them together, or name each one you are leaving and why. Bring every piece of prose the change touches into agreement with the new behaviour, and write it as a description of the code as it now stands, never as a narration of what changed. Delete anything the fix made unreachable. And run the project's own checks — format, lint, typecheck, tests, build — before you call an issue done.

AN ISSUE YOU CANNOT REPRODUCE IS NOT A FAILURE OF THE RUN — it is a result, and it has to be reported as one. Never paper over it with a speculative change: no fix goes in on a bug you were unable to make happen. Say exactly what you tried (the steps, the version, the environment) and what you observed instead, so the gap is something I can act on.

Deliver each fix as its own focused pull request, opened as a DRAFT (`gh pr create --draft`) — ALWAYS draft, never a ready-for-review PR, and never mark one ready yourself. Each PR closes the issue it fixes: put a closing reference (`Fixes #<n>`) in its description, and give it a title and description that name the defect and quote the evidence — the reproduction that proved the bug, and the same reproduction re-run clean after the fix. SHIP A REGRESSION TEST WITH EVERY FIX, one that pins the exact behaviour the fix corrected: you established the wrong value when you reproduced the bug and the right one when you confirmed the fix, so assert the right one in a way the wrong one would VIOLATE (an exact value, count, order, type or status code — never an incidental property like `>= 1` or `contains something`, which the unfixed code already satisfied). Do not undo or weaken the fix to check that; argue it from the reproduction you already captured. RIGHT BEFORE you open each PR, verify it is NOT a DUPLICATE of an existing open PR: enumerate the repo's open PRs and compare their ACTUAL CODE, not their titles or descriptions (those routinely mislead) — read each plausible candidate's real diff with `gh pr diff <n>`. If one already lands the same fix, do NOT open a duplicate: skip it and tell me which PR already covers it. You MAY bundle fixes for closely-related issues into one PR when they share a root cause or area — that is better than splitting hairs — but never one mega-PR of unrelated fixes, and never a PR for a fix whose reproduction you could not run clean.

Report the outcome ON THE ISSUE ITSELF, one comment per issue you actually worked (`gh issue comment <n> --repo software-mansion/argent`), so the reporter learns what happened without waiting for me. Say what you reproduced, what the cause turned out to be, and where the fix is — the PR number when there is one. If you could not reproduce it, say what you tried and what you saw instead, and ask for the one specific detail that would let you get further. Never post a bare "fixed" or "cannot reproduce" with nothing behind it, and never comment on an issue you did not work.

No AI attribution anywhere in git/GitHub — commits and PRs authored as me, no Co-Authored-By, no "Generated with" taglines.

Diplomat attribution tag — a REQUIRED exception to the no-attribution rule, for GitHub commentary only. Do NOT tag PR titles — leave every pull-request title exactly as you would write it. End every pull-request DESCRIPTION with a footer: after the body, a blank line, a horizontal rule on its own line (`---`), a blank line, then this exact last line — `<sub>Made by Diplomat. Like what you're seeing? [Give it a try](https://github.com/latekvo/Diplomat).</sub>`. In that footer the word Diplomat is PLAIN TEXT (not a link); ONLY "Give it a try" is a Markdown link, pointing to https://github.com/latekvo/Diplomat. For every standalone comment you post instead (an issue comment, or a review / reply comment), BEGIN it with the exact prefix `\[[Diplomat](https://github.com/latekvo/Diplomat)\]: `, copied character for character — the `\[` and `\]` are literal brackets, only the word Diplomat is a Markdown link (to https://github.com/latekvo/Diplomat), anything after that link is plain text, and a colon and a space close it — followed by the text you would otherwise have written, so it renders as "[Diplomat]: <your text>". This is the ONE sanctioned attribution; it applies to posted PR-description / comment text ONLY and never changes PR titles, commit messages, commit authorship, or the no-Co-Authored-By / no-"Generated with" git rules, which remain in force.

At the very end, give me one consolidated report covering EVERY issue the scope above put in front of you, with each one in exactly one bucket: fixed (with the reproduction you ran and where the fix landed), reproduced but not fixed (and what stopped you), could not reproduce (what you tried, what you saw), and skipped (which filter took it out — assigned to someone, not a bug, out of scope). An issue that fell out of the scope silently is a gap in this report, not an issue that did not exist.