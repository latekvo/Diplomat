In software-mansion/argent, go through every currently-open issue, and handle EACH one.

Enumerate them with `gh issue list --repo software-mansion/argent --state open --limit 200 --json number,title,author,assignees,labels,createdAt`.

ONLY REAL BUG REPORTS ARE IN SCOPE. For every issue in front of you, decide what it actually is: a bug report describes something that behaves wrongly. SKIP every feature request, enhancement proposal, design discussion, question, support request and wishlist item entirely — they are explicitly OUT OF SCOPE and must not be worked, however easy one looks. When an issue is ambiguous, treat it as out of scope and say so in the summary rather than implementing something nobody asked you to.

For each issue: build a CONCRETE REPRODUCTION from the report before you touch anything, develop the fix, and re-run that SAME reproduction to confirm it lands. Then put your own fix through the review moves in my CLAUDE.md with a swarm of agents (3–8) — claims vs code, nearest twin, non-happy paths, inputs, reachability both ways, and what outlives the call — plus the absence pass (a sibling that has it, prose that promises it, symmetry, and the mutation). Treat what the swarm returns as leads, not verdicts: confirm each against the code, fix what is real, and keep dispatching fresh swarms at that issue until one full pass comes back clean.

Hold to the bar in my CLAUDE.md throughout. DONE MEANS OBSERVABLE PROOF, never "should work": a fix counts only once the same reproduction that showed the bug is re-run and comes back clean, and you say which verification you ran. Fix the CLASS, not the instance — search the repo for every other call site, sibling backend or adjacent path of the same shape and fix them together, or name each one you are leaving and why. Bring every piece of prose the change touches into agreement with the new behaviour, and write it as a description of the code as it now stands, never as a narration of what changed. Delete anything the fix made unreachable. And run the project's own checks — format, lint, typecheck, tests, build — before you call an issue done.

AN ISSUE YOU CANNOT REPRODUCE IS NOT A FAILURE OF THE RUN — it is a result, and it has to be reported as one. Never paper over it with a speculative change: no fix goes in on a bug you were unable to make happen. Say exactly what you tried (the steps, the version, the environment) and what you observed instead, so the gap is something I can act on.

This run opens NO pull requests and pushes NOTHING: do not commit, do not push, do not open a PR. Keep each fix in the working tree and hand it to me in the summary — which files it touches, what it changes, and the reproduction you ran before and after it. Nothing reaches the remote.

At the very end, give me one consolidated report covering EVERY issue the scope above put in front of you, with each one in exactly one bucket: fixed (with the reproduction you ran and where the fix landed), reproduced but not fixed (and what stopped you), could not reproduce (what you tried, what you saw), and skipped (which filter took it out — assigned to someone, not a bug, out of scope). An issue that fell out of the scope silently is a gap in this report, not an issue that did not exist.