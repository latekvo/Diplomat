Take issue #421 in software-mansion/argent, and work that ONE issue — nothing else in the repo's issue list is this run's business. Use the `gh` CLI to fetch it (`gh issue view 421 --repo software-mansion/argent --comments`), including every comment on it — a follow-up comment routinely carries the reproduction, the version, or the correction the opening post lacks.

THIS ISSUE CAME OUT OF A SWEEP, and its turn may have come hours after the sweep picked it up. Before you touch anything, read its CURRENT state off the fetch above: if it has been closed since, somebody has already dealt with it — stop there, change nothing, post nothing, and report it as skipped.

ONLY A REAL BUG REPORT IS IN SCOPE. Decide what this issue actually is: a bug report describes something that behaves wrongly. A feature request, enhancement proposal, design discussion, question, support request or wishlist item is explicitly OUT OF SCOPE and must not be worked, however easy it looks — stop there and report it as skipped. When it is ambiguous, treat it as out of scope and say so rather than implementing something nobody asked you to.

Build a CONCRETE REPRODUCTION from the report before you touch anything, develop the fix, and re-run that SAME reproduction to confirm it lands. Then put your own fix through the review moves in my CLAUDE.md with a swarm of agents (3–8) — claims vs code, nearest twin, non-happy paths, inputs, reachability both ways, and what outlives the call — plus the absence pass (a sibling that has it, prose that promises it, symmetry, and the mutation). Treat what the swarm returns as leads, not verdicts: confirm each against the code, fix what is real, and keep dispatching fresh swarms until one full pass comes back clean.

Hold to the bar in my CLAUDE.md throughout. DONE MEANS OBSERVABLE PROOF, never "should work": the fix counts only once the same reproduction that showed the bug is re-run and comes back clean, and you say which verification you ran. Fix the CLASS, not the instance — search the repo for every other call site, sibling backend or adjacent path of the same shape and fix them together, or name each one you are leaving and why. Bring every piece of prose the change touches into agreement with the new behaviour, and write it as a description of the code as it now stands, never as a narration of what changed. Delete anything the fix made unreachable. And run the project's own checks — format, lint, typecheck, tests, build — before you call it done.

AN ISSUE YOU CANNOT REPRODUCE IS NOT A FAILURE OF THE RUN — it is a result, and it has to be reported as one. Never paper over it with a speculative change: no fix goes in on a bug you were unable to make happen. Say exactly what you tried (the steps, the version, the environment) and what you observed instead, so the gap is something I can act on.

This run opens NO pull requests and pushes NOTHING: do not commit, do not push, do not open a PR. Keep the fix in the working tree and hand it to me in the report — which files it touches, what it changes, and the reproduction you ran before and after it. Nothing reaches the remote.

At the very end, give me one report on issue #421 — it is the whole of this run's work, so put it in exactly one bucket: fixed (with the reproduction you ran and where the fix landed), reproduced but not fixed (and what stopped you), could not reproduce (what you tried, what you saw), or skipped (which rule above took it out — closed since, claimed by somebody else, not a bug).